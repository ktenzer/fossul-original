# Go
# Build your Go project.
# Add steps that test, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/go

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

variables:
  GOPATH: '$(system.defaultWorkingDirectory)' # Go workspace path
  GOBIN:  '$(GOPATH)/bin' # Go binaries path
  PLUGIN_DIR: '$(GOBIN)'
#  GOBIN:  '$(GOPATH)/bin' # Go binaries path
#  GOROOT: '/usr/local/go1.11' # Go installation path
#  GOPATH: '$(system.defaultWorkingDirectory)/gopath' # Go workspace path
  modulePath: '$(GOPATH)' # Path to the module's code

steps:
- script: |
    echo "GOBIN $(GOBIN)"
    echo "GOPATH $(GOPATH)"
    echo "code $(GOPATH)/src/github.com/$(build.repository.name)"
    #ls -R $(GOPATH)
    mkdir -p '$(GOBIN)'
    mkdir -p '$(GOPATH)/pkg'
    mkdir -p '$(modulePath)/src/fossul'
    shopt -s extglob
    shopt -s dotglob
    mv  $(GOPATH) '$(modulePath)/src/fossul'
    ls -R '$(modulePath)'
    echo '##vso[task.prependpath]$(GOBIN)'
    echo '##vso[task.prependpath]$(GOROOT)/bin'
  displayName: 'Set up the Go workspace'

- script: |
    go version
    go get -v -t -d ./...
    if [ -f Gopkg.toml ]; then
        curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh
        dep ensure
    fi
    #$(GOPATH)/fossul-build.sh
    go test engine/util
    #go build -v .
  workingDirectory: '$(modulePath)'
  displayName: 'Get dependencies, then build'
